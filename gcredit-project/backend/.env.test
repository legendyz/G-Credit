# Test Environment Configuration
# Story 8.8: E2E Test Isolation - Higher rate limits for tests

# Rate Limiting - Higher limits for E2E testing
RATE_LIMIT_TTL=60000
RATE_LIMIT_MAX=1000

# ARCH-C3: Upload endpoint rate limit
# NOTE: UPLOAD_THROTTLE_LIMIT/TTL cannot be overridden here because
# @Throttle decorator evaluates process.env at class-load time,
# before ConfigModule processes .env.test. The default (10/5min) applies everywhere.
