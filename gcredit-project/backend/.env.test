# Test Environment Configuration
# Story 8.8: E2E Test Isolation - Higher rate limits for tests

# Rate Limiting - Higher limits for E2E testing
RATE_LIMIT_TTL=60000
RATE_LIMIT_MAX=1000

# Story 15.13: Override ALL @Throttle() per-endpoint limits for E2E tests
# ConfigurableThrottlerGuard reads these in onModuleInit (after ConfigModule loads .env.test)
THROTTLE_TTL_SECONDS=60
THROTTLE_LIMIT=1000
