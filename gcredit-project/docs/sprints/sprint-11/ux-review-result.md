# Sprint 11 UX Review Result

**Reviewer:** Sally (UX Designer)  
**Requested by:** SM (Bob)  
**Date:** 2026-02-13  
**Sprint:** Sprint 11 — Security + Quality + Feature Hardening  
**Target Version:** v1.1.0

---

## 📌 Overall Verdict: APPROVED WITH CONDITIONS

Sprint 11 涉及 7 个 UI 相关 story，覆盖隐私控制、社交分享、错误页面、数据导出、导航修复、信息脱敏和数据展示优化。以下是逐项审核的 UX 建议和交互方案推荐。

---

## 1. FR19: Badge Visibility Toggle（Story 11.4）

### 📍 当前状态
- 后端无 `PATCH /api/badges/:id/visibility` 端点
- `BadgeTimelineCard.tsx` 和 `BadgeDetailModal` 中均无可见性切换控件
- 此功能完全缺失，属于 P0 隐私控制特性

### ✅ UX 建议

#### Toggle 组件位置

**推荐方案：双入口设计**

| 位置 | 形式 | 理由 |
|------|------|------|
| **Badge Wallet 卡片**（主入口） | 在 `BadgeTimelineCard` 右侧操作区增加一个小型 `Switch` 开关，紧挨现有 View/Download 图标下方 | 用户在浏览 wallet 时即可快速切换，无需进入详情——这是最高频的使用场景 |
| **Badge Detail Modal**（确认入口） | 在 Modal footer 区域增加 "Visibility: Public/Private" 切换，带状态文字标签 | 用户查看详情时可以确认和修改设置，提供更多上下文说明 |

**不推荐：** 仅放在 Wallet 列表页。列表页操作空间有限，且 toggle 会与已有的 View/Download 操作竞争注意力。

#### 具体交互方案

```
Badge Timeline Card 布局调整：
┌──────────────────────────────────────────────┐
│ [Badge Image]  Badge Name                    │
│                Issued by John Doe            │
│                [CLAIMED] [Category]    👁️    │
│                                        ⬇️    │
│                                      🔒/🌐   │  ← 新增: Visibility Toggle
└──────────────────────────────────────────────┘
```

- **Toggle 图标：** 使用 Lucide `Globe`（公开）/ `Lock`（私密），不要使用 emoji
- **交互：** 点击后直接 PATCH 切换，无需二次确认弹窗（操作可逆）
- **反馈：** 使用 `toast.success()` 显示 "Badge set to Private" 或 "Badge set to Public"，与现有项目 toast 模式一致（Sonner）
- **状态变化：** Toggle 切换后，图标即时更新，无需页面刷新
- **Loading 状态：** Toggle 期间显示 spinner 替换图标，防止重复点击

#### 默认值

**推荐：默认 PUBLIC**

| 考量 | 分析 |
|------|------|
| G-Credit 定位 | 企业内部数字证书平台，badge 的价值在于被看到和验证 |
| PRD 定位 | "viral growth engine" — 默认公开才能驱动分享增长 |
| 隐私保护 | 员工可以随时切换为 PRIVATE，操作简单可逆 |
| 竞品参考 | Credly / Badgr 均默认公开 |

**结论：** 默认 PUBLIC 合理。但在 **ClaimSuccessModal** 中应增加一句提示："Your badge is publicly visible. You can change this anytime from your wallet."

#### 批量切换

**推荐：Sprint 11 不做批量切换，作为 Sprint 12 优化项**

理由：
- 单个 toggle 覆盖 90% 使用场景
- 批量操作增加 UI 复杂度（全选、部分选、确认弹窗）
- Sprint 11 时间预算紧张（4-6h）

若未来实现，建议在 Wallet 页增加 "Select Mode" 按钮，进入多选模式后顶部出现 "Set All Private / Set All Public" 操作栏。

#### 切换反馈

- **成功：** `toast.success("Badge visibility updated to Private/Public")`
- **失败：** `toast.error("Failed to update visibility. Please try again.")`
- **无动画需求：** Toggle 状态变化本身已足够直观，不需额外动画

#### Private Badge 在公开 Profile 的呈现

**推荐方案："X badges hidden" 提示**

```
┌─ Public Profile ────────────────────────────────┐
│  [Badge A]  [Badge B]  [Badge C]                │
│                                                  │
│  🔒 2 badges are set to private by the owner    │
└──────────────────────────────────────────────────┘
```

- 完全隐藏会让访问者觉得此人 badge 很少，影响信任感
- 显示 "X badges hidden" 表明还有更多成就，同时尊重隐私选择
- 不透露 private badge 的任何具体信息（名称、类型）

#### Private Badge 验证链接的 404 页面

当访问者通过直接 URL 访问 PRIVATE badge 的验证页时：

**推荐措辞：**
> **Badge Not Available**  
> This badge's visibility has been set to private by the badge holder. The verification link is currently not accessible.  
> If you believe this is an error, please contact the badge holder directly.

**注意：** 使用 404 状态码（而非 403），避免泄露 badge 是否存在。页面样式与现有 `NotFoundPage.tsx` 保持一致（使用 `text-neutral-*` 设计 token，`bg-brand-600` 按钮）。

### ⚠️ UX 一致性风险
- `BadgeTimelineCard` 目前使用 emoji（👁️、⬇️）作为操作图标，新增的 Visibility toggle 应使用 Lucide 图标（`Globe` / `Lock`）。建议同时将已有 emoji 替换为 Lucide 图标（`Eye`、`Download`），保持一致——此工作可与 Story 11.15 Design System Consistency 合并。

### 📱 移动端适配
- Toggle 图标 touch target 保持 44×44px（与现有操作图标一致）
- 在移动端卡片布局中，toggle 放在卡片底部操作栏，与 View/Download 排列成一行

---

## 2. LinkedIn Share Tab（Story 11.5）

### 📍 当前状态
- `BadgeShareModal.tsx`（786 行）有 3 个 tab：Email、Teams、Widget
- Tab 使用 `role="tablist"` + keyboard navigation（Arrow 键切换），WCAG 合规
- 现有 tab 样式：`flex-1 min-h-[44px]` + active 状态蓝色下边框 + emoji 图标
- 无 LinkedIn 分享功能

### ✅ UX 建议

#### Tab 结构调整

**4 Tab 布局：** Email → LinkedIn → Teams → Widget

| Tab | 图标 | 排序理由 |
|-----|------|---------|
| 📧 Email | emoji（保持现有） | 最常用分享方式 |
| 🔗 LinkedIn | LinkedIn 品牌图标（SVG） | 第二高频社交分享 — PRD "virality engine" |
| 👥 Teams | emoji（保持现有） | 内部沟通渠道 |
| 🔗 Widget | emoji（保持现有） | 嵌入式分享（最低频） |

**LinkedIn Tab 图标建议：** 使用 LinkedIn 官方品牌色 `#0A66C2` 的 SVG logo（"in" 徽标），不使用 emoji。这是与其他 tab 的唯一视觉差异，但 LinkedIn 品牌识别度要求使用官方图标。

> **注：** 长期来看（Story 11.15），所有 tab 图标应统一迁移到 Lucide 图标（`Mail`、`Linkedin`、`Users`、`Code2`）。Sprint 11 先添加 LinkedIn tab，图标统一作为后续优化。

#### LinkedIn Tab 布局

```
┌─ LinkedIn Tab Panel ────────────────────────────┐
│                                                  │
│  📋 Share Preview                               │
│  ┌──────────────────────────────────────────┐   │
│  │ [Badge Image]                            │   │
│  │ 🏆 I earned the "Cloud Architecture"    │   │
│  │    badge on G-Credit!                    │   │
│  │                                          │   │
│  │ Verify: https://gcredit.com/verify/xxx  │   │
│  │                                          │   │
│  │ #DigitalCredentials #ProfessionalDev    │   │
│  └──────────────────────────────────────────┘   │
│                                                  │
│  ✏️ Customize Message (optional)                │
│  ┌──────────────────────────────────────────┐   │
│  │ [Editable text area with default text]   │   │
│  └──────────────────────────────────────────┘   │
│                                                  │
│  [ 🔗 Share on LinkedIn ]  ← Primary Button    │
│                                                  │
│  ℹ️ Opens LinkedIn in a new window              │
└──────────────────────────────────────────────────┘
```

#### 分享文案模板

```
Title:     "I earned the "{badgeName}" digital badge!"
Text:      "I'm proud to have earned the {badgeName} badge, issued by {issuerName} via G-Credit. 
            This credential validates my skills in {skillNames}. 
            Verify my badge: {verificationUrl}"
Hashtags:  #DigitalCredentials #ProfessionalDevelopment #GCredit
```

- 文案区域可编辑（textarea），让员工个性化
- 默认填充模板，降低分享门槛
- 验证链接自动附加，不可编辑（确保真实性）

#### 技术实现

使用 LinkedIn Share URL API（无需 OAuth）：
```
https://www.linkedin.com/sharing/share-offsite/?url={verificationUrl}
```

- `window.open()` 打开 600×600 弹窗
- `url` 参数为 badge 的公开验证链接（确保 OG meta tags 正确）
- LinkedIn 会自动从 URL 抓取 Open Graph 信息（title, description, image）

**前提条件：** 验证页（`VerifyBadgePage`）需要在 `<head>` 中注入 Open Graph meta tags：
```html
<meta property="og:title" content="Verified Badge: Cloud Architecture" />
<meta property="og:description" content="Issued by GCredit to John Doe" />
<meta property="og:image" content="{badgeImageUrl}" />
<meta property="og:url" content="{verificationUrl}" />
```
如果当前验证页无 OG tags，需在 Story 11.5 中一并处理（SSR 或动态 meta tag 注入）。

#### 分享成功后的反馈

- LinkedIn share 弹窗由用户手动关闭，应用无法检测分享是否完成
- **推荐：** 点击 "Share on LinkedIn" 后，按钮文字变为 "✓ LinkedIn opened — share from there"（disabled 状态），5 秒后恢复
- 不关闭 BadgeShareModal，让用户可以继续通过其他渠道分享

#### 分享事件追踪

- 点击 "Share on LinkedIn" 时记录分析事件（不等待分享完成）
- 事件：`POST /api/analytics/track` with `{ type: 'SHARE', channel: 'LINKEDIN', badgeId }`

### 📱 移动端适配
- 4 个 tab 在 375px 屏幕上每个约 93px 宽——空间足够显示图标 + 短文字
- 如果文字太长，使用 "仅图标" 模式在 < 360px 屏幕上
- LinkedIn tab 内容区域使用 `overflow-y-auto`，预览区可滚动

### ⚠️ UX 一致性风险
- LinkedIn tab 使用品牌色 SVG 而其他 tab 使用 emoji — **可接受的不一致**，品牌识别优先。Sprint 11.15 统一后此差异消失。
- 4 tab 布局需更新 `TABS` 常量和 keyboard navigation 的 `ArrowLeft/ArrowRight` 逻辑。

---

## 3. 403 Access Denied Page（Story 11.19）

### 📍 当前状态
- 仅有 `NotFoundPage.tsx`（26 行）作为通配错误页
- 403 场景显示 generic error message，无专用页面
- `NotFoundPage` 使用 `Layout` 包裹 + `text-neutral-*` 设计 token + `bg-brand-600` 按钮

### ✅ UX 建议

#### 信息层次

```
┌─────────────────────────────────────────────────────┐
│                                                      │
│              🔒                                      │  ← Lock icon (Lucide ShieldAlert)
│                                                      │
│           403                                        │  ← 大号数字，neutral-300
│                                                      │
│     Access Denied                                    │  ← 标题，neutral-700
│                                                      │
│  You don't have permission to access this page.      │  ← 说明文字，neutral-500
│  Your current role ({role}) does not have access      │
│  to this resource.                                    │
│                                                      │
│  [ ← Go Back ]          [ Contact Admin ]            │
│   (outline)               (brand-600)                │
│                                                      │
└─────────────────────────────────────────────────────┘
```

#### 是否显示角色信息？

**推荐：显示当前角色，不显示所需角色**

| 信息 | 显示？ | 理由 |
|------|--------|------|
| 当前用户角色 | ✅ 是 | 帮助用户理解 "为什么我不能访问"，减少困惑 |
| 所需角色 | ❌ 否 | 暴露授权策略细节是安全风险（OWASP 建议避免） |

文案：`"Your current role (Employee) does not have access to this resource."`

#### 按钮设计

| 按钮 | 样式 | 行为 |
|------|------|------|
| "Go Back" | `variant="outline"` + ← 箭头图标 | `navigate(-1)` — 返回上一页。如果无浏览历史，fallback 到 `navigate('/')` |
| "Contact Admin" | `bg-brand-600 text-white` (Primary) | 打开 `mailto:admin@company.com?subject=Access Request: {pathname}` |

#### 与 404 页面的视觉一致性

直接复用 `NotFoundPage.tsx` 的布局模式：
- 同样使用 `Layout` 组件包裹 + `pageTitle`
- 同样的 `flex flex-col items-center justify-center min-h-[60vh] text-center px-4` 容器
- 同样的字体层次：`text-6xl font-bold text-neutral-300` (数字) → `text-2xl font-semibold text-neutral-700` (标题) → `text-neutral-500 mb-8 max-w-md` (描述)
- 按钮使用相同的 `px-6 py-3 rounded-lg min-h-[44px]` 尺寸

唯一差异：
- 图标从无 → `ShieldAlert`（Lucide）
- 按钮从单个 → 两个并排
- 文案内容不同

#### 401 vs 403 的区分

**推荐：区分处理**

| 状态码 | 含义 | 处理方式 |
|--------|------|---------|
| 401 | 未登录 / Token 过期 | 现有逻辑：authStore 自动 redirect 到 `/login`（不需要新页面） |
| 403 | 已登录但无权限 | **新页面** `/403` 或 `AccessDeniedPage.tsx` |

当前 `ProtectedRoute` 对未认证用户已有重定向逻辑，403 页面仅处理 "已登录但角色不足" 的场景，两者不冲突。

### 📱 移动端适配
- 两个按钮在移动端纵向堆叠：`flex-col sm:flex-row gap-3`
- 403 数字字号可适当缩小：`text-5xl md:text-6xl`

### ⚠️ UX 一致性风险
- 无风险。此页面直接复用 NotFoundPage 模式，视觉语言完全一致。

---

## 4. Analytics CSV Export（Story 11.17）

### 📍 当前状态
- `AdminAnalyticsPage.tsx`（319 行）使用 `PageTemplate` 组件
- 页面结构：`PageTemplate` 标题行 → KPI Cards → Issuance Trends (带 period selector) → Top Performers + Skills Distribution → Recent Activity → Footer (Last Updated + Refresh)
- `PageTemplate` 支持 `actions` prop（渲染在标题行右侧）
- 无任何导出功能

### ✅ UX 建议

#### 导出按钮位置

**推荐：PageTemplate 标题行右侧（`actions` slot）**

```
┌─ Admin Analytics ────────────────── [ ⬇ Export CSV ] ─┐
│  System-wide overview of users, badges...              │
│                                                         │
│  [KPI Cards...]                                         │
```

理由：
- `PageTemplate` 已有 `actions` prop 专门用于页级操作（与 Refresh 按钮意义不同）
- 标题行右侧是 "页面级操作" 的标准位置（与 Bulk Issuance 页面的 "Download Template" 按钮位置一致）
- 不应放在固定底部栏（analytics 是只读浏览页，不需要 sticky 操作栏）
- 不应放在筛选区（导出是一次性操作，不是反复调整的筛选条件）

#### 导出触发交互

**推荐：直接下载（浏览器原生下载）**

| 方案 | 适用场景 | 推荐？ |
|------|---------|--------|
| 直接下载 | 数据量 < 10MB，生成时间 < 3 秒 | ✅ 推荐 |
| 进度提示 | 大数据集，生成时间 > 5 秒 | 未来优化 |
| 邮件发送 | 超大数据集 | 不需要（MVP 数据量小） |

交互流程：
1. 点击 "Export CSV" → 按钮变为 loading 状态（spinner + "Exporting..."）
2. 后端返回 CSV 文件 → 浏览器自动下载
3. 按钮恢复正常 + `toast.success("CSV exported successfully")`
4. 失败时 → `toast.error("Export failed. Please try again.")`

#### 导出范围

**Sprint 11 推荐：导出当前页面的所有数据（不需要额外选择器）**

导出内容应包含页面上可见的所有数据：
- System Overview KPIs
- Issuance Trends（按当前选择的 period）
- Top Performers
- Skills Distribution
- Recent Activity

如果未来需要选择器，可以在 Export 按钮旁增加下拉菜单（Sprint 12+）。

#### 按钮样式

**推荐：`variant="outline"` + Download 图标**

```tsx
<Button variant="outline" className="gap-2">
  <Download className="h-4 w-4" />
  Export CSV
</Button>
```

- `outline` 样式（非 primary）：导出是辅助操作，不是页面主行为
- 与 Refresh 按钮视觉权重一致，但位置在标题行更突出
- 使用 Lucide `Download` 图标（与项目其他下载操作一致）

#### 文件名格式

**推荐：** `gcredit-analytics-{YYYY-MM-DD}.csv`

示例：`gcredit-analytics-2026-02-13.csv`

- 前缀标识来源（gcredit）
- 日期标识导出时间（方便文件管理）
- 无需包含 period（文件内容有日期列）

### 📱 移动端适配
- `PageTemplate` 的 `actions` 在移动端自动换行到标题下方（`flex-col sm:flex-row`）
- Export 按钮全宽显示：`w-full sm:w-auto`

### ⚠️ UX 一致性风险
- 无风险。使用 PageTemplate `actions` slot 是已有模式，按钮样式遵循 shadcn/ui 规范。

---

## 5. User Management 导航（Story 11.23）

### 📍 当前状态

| 平台 | 当前标签 | 位置 |
|------|---------|------|
| Desktop (`Navbar.tsx`) | "Users" | Admin-only，在 Analytics 后面 |
| Mobile (`MobileNav.tsx`) | "User Management" | Admin-only，在 Analytics 后面 |

**问题：** 标签文字不一致 — Desktop 显示 "Users"，Mobile 显示 "User Management"。

### ✅ UX 建议

#### 标签文本推荐

**推荐："Users"**

| 选项 | 优点 | 缺点 |
|------|------|------|
| "Users" ✅ | 简洁，与其他 nav 项长度一致（Dashboard, Analytics） | 可能与 "My Profile" 混淆 |
| "User Management" | 更明确功能 | 在 Desktop nav 中太长，挤压其他项 |
| "Manage Users" | 动词开头，暗示管理功能 | 与其他 nav 项（名词式）风格不一致 |

**理由：** 
- Desktop 导航已有 8 项链接，空间紧张，短标签更好
- "Users" 配合 ADMIN-only 可见性已足够区分
- 与其他 nav 项命名风格一致（名词：Dashboard, Analytics, Profile）

#### 统一变更方案
- `MobileNav.tsx` line 中 `label: 'User Management'` → `label: 'Users'`
- `Navbar.tsx` 保持现有 "Users" 不变

#### 导航图标

当前 Desktop `Navbar.tsx` 使用纯文字链接（无图标），Mobile `MobileNav.tsx` 也是纯文字。

**推荐：Sprint 11 不添加图标（保持现有一致性）**。如果未来为所有 nav 项加图标，User Management 推荐使用 Lucide `Users` 图标。

#### 位置顺序

当前 Desktop 顺序：Dashboard → My Wallet → Badge Templates → Badge Management → Bulk Issuance → Analytics → Users → Profile

**推荐：保持现有顺序不变**

理由：
- "Users" 是 ADMIN-only 功能，放在通用功能之后合理
- 与 Analytics 相邻（两者都是管理功能）形成 "管理" 功能组
- 移动到前面会打破用户已有的导航肌肉记忆

#### ADMIN-only 视觉区分

**推荐：Sprint 11 不需要额外标识**

理由：
- "Users" 链接仅对 ADMIN 角色可见（条件渲染），其他角色完全看不到
- 不需要 "Admin" 灰色标签或分隔线——增加视觉噪音
- 如果未来有更多 ADMIN-only 页面（Settings, Audit Log），可以考虑分组

### 📱 移动端适配
- 标签统一为 "Users" 后，移动端无额外适配需求
- 现有 44px touch target 和 focus-visible ring 已满足标准

### ⚠️ UX 一致性风险
- 无风险。仅标签文字统一，无布局或样式变更。

---

## 6. Issuer Email Masking（Story 11.7）

### 📍 当前状态
- `VerifyBadgePage.tsx` line 259：`<div className="text-sm text-neutral-500">{badge.issuer.email}</div>` — 直接展示完整邮箱
- 同时 Recipient email（line 248）也是明文展示
- 后端已有 `maskEmail()` 工具函数但未应用于验证端点

### ✅ UX 建议

#### 脱敏格式

**推荐格式：** `j***@company.com`（首字母 + *** + @ + 完整域名）

| 格式 | 示例 | 可读性 | 推荐？ |
|------|------|--------|--------|
| `j***@company.com` | 首字母 + 掩码 + 完整域名 | ✅ 高 — 可识别来源组织 | ✅ 推荐 |
| `j****n@company.com` | 首尾字母 + 掩码 | 中 | 可选 |
| `****@company.com` | 全掩码 + 域名 | 低 — 无法确认发件人 | ❌ |
| `j***@c***.com` | 全部掩码 | 极低 | ❌ |

完整域名的保留至关重要——验证页的核心功能是建立信任，访客需要知道 badge 来自哪个组织。

#### 是否需要 Tooltip？

**推荐：不需要 Tooltip，使用页面底部信任声明**

理由：
- Tooltip 在移动端体验差（需要 hover/long-press）
- 验证页面向外部访客，他们对 G-Credit 不熟悉，不会主动 hover
- 更好的方案：在页面底部（Download 按钮下方）添加一句信任声明

```
"Personal information is partially hidden to protect privacy. 
 Badge authenticity is verified by G-Credit's cryptographic signature."
```

这句话同时解决了 email masking 的困惑，也增强了整体信任感。

#### 脱敏范围

| 字段 | 当前显示 | Story 11.7 范围 | 建议 |
|------|---------|----------------|------|
| Issuer email | 明文 | ✅ 需脱敏 | `j***@company.com` |
| Recipient email | 明文 | **建议一并脱敏** | `j***@company.com` |
| Recipient name | 明文 | 保持明文 | 验证页核心信息，必须显示 |
| Issuer name | 明文 | 保持明文 | 验证页核心信息，必须显示 |

**重要建议：** Recipient email 也应脱敏。公开验证页面上展示完整个人邮箱是隐私风险，建议在 Story 11.7 中一并处理（后端 maskEmail 函数可复用，额外工作量 < 5 分钟）。

#### 对信任感的影响评估

| 因素 | 影响 | 缓解措施 |
|------|------|---------|
| 邮箱部分隐藏 → 访客无法联系 issuer | 低 — 验证页不是联系渠道 | 保留完整域名 |
| 访客怀疑信息真实性 | 极低 — 有 Open Badges JSON-LD 下载 | 信任声明文案 |
| 与其他平台体验差异 | 无 — Credly 等也脱敏 | 行业标准做法 |

**结论：** 脱敏对信任感影响极小。保留完整域名 + 信任声明足以弥补。

### 📱 移动端适配
- 无额外适配需求。脱敏后的邮箱字符串更短，在小屏幕上反而更好显示。

### ⚠️ UX 一致性风险
- 低风险。脱敏仅影响公开验证页，不影响内部管理界面。

---

## 7. Verification Skill UUID→Name（Story 11.18）

### 📍 当前状态
- `VerifyBadgePage.tsx` line 267-275：Skills 在 `badge.badge.skills` 数组中以 `skillId` 字符串渲染
- 当前使用 Chip/Tag 样式：`px-3 py-1 bg-brand-100 text-brand-800 rounded-full text-sm`
- 如果 skills 数组包含 UUID 字符串，则显示为 `a1b2c3d4-e5f6-...`

### ✅ UX 建议

#### Skill 名称展示格式

**推荐：保持现有 Chip/Tag 样式（`rounded-full`）**

```
Skills & Competencies
[Cloud Architecture]  [Security]  [DevOps]  [Kubernetes]
```

理由：
- 当前已是 Chip 样式，只是内容从 UUID 变为名称——**布局无需变更**
- Chip/Tag 样式比逗号分隔列表更容易扫描
- 与 badge 详情页（`BadgeDetailModal`）的 skill 展示保持一致（如果那里也用 Chip）

**不推荐：** 逗号分隔列表。"Cloud Architecture, Security, DevOps, Kubernetes" 在多技能场景下可读性差，且与卡片化的设计语言不匹配。

#### 与 Badge Detail Modal 的一致性

需确认 `BadgeDetailModal` 中的 skill 展示样式：
- 如果也使用 Chip（`rounded-full` + `bg-brand-100`）→ 完美一致，无需额外工作
- 如果使用不同样式 → 应统一为同一样式（以 VerifyBadgePage 的 Chip 样式为准）

#### 长技能名称处理

- Chip 应使用 `max-w-[200px] truncate` 防止超长名称撑破布局
- 超过 6 个技能时，考虑两行折叠 + "Show all X skills" 展开

### 📱 移动端适配
- Chip 使用 `flex-wrap gap-2`（当前已是），在窄屏幕上自然换行
- 无额外适配需求

### ⚠️ UX 一致性风险
- **低风险。** 布局完全不变，仅数据层变更（UUID → 名称）。

---

## 📊 审核汇总

### 各项审核结论

| # | Story | 审核项 | 结论 | 条件（如有） |
|---|-------|--------|------|-------------|
| 1 | 11.4 | Badge Visibility Toggle | ✅ APPROVED WITH CONDITIONS | Toggle 放 Wallet 卡片 + Detail Modal 双入口；默认 PUBLIC；使用 Lucide 图标；PRIVATE badge 在公开页显示 "X badges hidden" |
| 2 | 11.5 | LinkedIn Share Tab | ✅ APPROVED WITH CONDITIONS | 新增第 4 个 tab（Email → LinkedIn → Teams → Widget）；验证页需 OG meta tags；分享后不关闭 modal |
| 3 | 11.19 | 403 Access Denied Page | ✅ APPROVED | 复用 NotFoundPage 布局模式；显示当前角色；不显示所需角色；双按钮设计 |
| 4 | 11.17 | Analytics CSV Export | ✅ APPROVED | 按钮在 PageTemplate `actions` slot；outline 样式 + Download 图标；直接下载 |
| 5 | 11.23 | User Management Nav | ✅ APPROVED | 统一为 "Users"；保持当前位置和样式 |
| 6 | 11.7 | Issuer Email Masking | ✅ APPROVED WITH CONDITIONS | `j***@company.com` 格式；建议同时脱敏 Recipient email；页面底部加信任声明 |
| 7 | 11.18 | Verification Skill UUID→Name | ✅ APPROVED | 保持现有 Chip/Tag 样式；仅替换内容 |

### 需要的线框图或 UI 规范

| 项 | 是否需要线框图 | 说明 |
|----|--------------|------|
| Badge Visibility Toggle | ❌ 不需要 | 上方 ASCII 图示 + 文字描述已足够清晰 |
| LinkedIn Share Tab | ❌ 不需要 | 复用现有 tab 结构，上方布局描述足够 |
| 403 Page | ❌ 不需要 | 直接复用 NotFoundPage 模式，代码级复用 |
| Analytics CSV Export | ❌ 不需要 | 使用 PageTemplate `actions` slot 一行代码 |
| 其他 | ❌ | 均为小范围变更 |

### 跨 Story UX 一致性风险汇总

| 风险 | 严重度 | 涉及 Story | 建议 |
|------|--------|-----------|------|
| BadgeTimelineCard 使用 emoji（👁️⬇️）与新增 Lucide toggle 图标不一致 | P2 | 11.4 + 11.15 | 在 Story 11.15 中统一迁移为 Lucide 图标 |
| BadgeShareModal tab 图标混用（emoji + LinkedIn SVG） | P2 | 11.5 + 11.15 | Sprint 11 可接受，11.15 统一 |
| VerifyBadgePage 使用 `axios` 而其他页面用 `fetch`/`useQuery` | P1 | — | 已在 11.15/12 规划中，不影响本次 UI 变更 |
| NotFoundPage 与 AccessDeniedPage 按钮样式需完全一致 | P2 | 11.19 | 开发时直接复用 NotFoundPage 的 className |

### 移动端适配总结

| Story | 移动端适配需求 | 工作量 |
|-------|--------------|--------|
| 11.4 Badge Visibility | Toggle 在卡片底部操作栏 44px | 已包含 |
| 11.5 LinkedIn Tab | 4 tab 自适应 + 文字省略 | 已包含 |
| 11.19 403 Page | 按钮纵向堆叠 + 数字字号调整 | 已包含 |
| 11.17 CSV Export | 按钮全宽 | 已包含 |
| 11.23 Nav Fix | 无额外需求 | — |
| 11.7 Email Masking | 无额外需求 | — |
| 11.18 Skill UUID→Name | 无额外需求 | — |

---

## ✅ 最终审核结论

**APPROVED WITH CONDITIONS**

Sprint 11 的 7 个 UI 相关 story 从 UX 角度整体合理，与现有设计系统和交互模式一致。主要条件：

1. **Story 11.4（Badge Visibility）** — 必须实现双入口（Wallet 卡片 + Detail Modal），默认 PUBLIC，PRIVATE badge 在公开页显示 "X badges hidden"
2. **Story 11.5（LinkedIn Tab）** — 验证页需要 Open Graph meta tags 才能保证 LinkedIn 抓取正确的预览内容
3. **Story 11.7（Email Masking）** — 建议将 Recipient email 脱敏纳入同一 story（工作量极小），并在验证页添加隐私信任声明

以上条件不影响 sprint 启动，可在各 story 开发过程中落实。

---

*Review completed 2026-02-13 by Sally (UX Designer Agent) per sprint-11/ux-review-brief.md*
