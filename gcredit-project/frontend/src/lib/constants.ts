/**
 * Shared UI constants — Story 12.8
 *
 * Neutral module for cross-layer constants that should not live
 * inside hooks, components, or utilities to avoid coupling.
 */

/** Fallback label displayed when a skill ID cannot be resolved to a name */
export const UNKNOWN_SKILL_LABEL = 'Unknown Skill';
